# 顶部右侧导航与弹框迁移指南（对标 pc_ui console_manage）

本文档整理官网（oortsite）siteNew 系列「登录后顶部右侧区域」的完整实现，供其他网站做同类迁移时直接参考。对标源：`pc_ui/src` console_manage 页面（commonPageHeader.vue + commonPageHeaderRightInfoaPaas.vue 及弹层组件）。

## 一、最终效果

登录后顶部右侧按钮组（从左到右）：

```
[全屏] [语言切换] 欢迎您 [头像+用户名▾] [九宫格] [菜单]
```

- 未登录：仅显示「登录/注册」按钮（跳 SSO 登录页）
- 四个弹框（语言/用户/九宫格/菜单）统一视觉：浅蓝底 `#EDF3F9`、壳圆角 10px、内容块圆角 4PX、项 hover `#dde3ea`、项间分隔线 `#e3e9f0`、每项右侧箭头、退出登录红字 `#FF3C3C`

## 二、前置依赖

| 依赖 | 说明 |
| --- | --- |
| element-plus | el-dropdown / el-popover / el-icon / ElMessage |
| @vueuse/core ≥ 8 | useFullscreen、useSessionStorage、StorageSerializers |
| ofetch | 接口请求（或替换为项目内 request 封装） |
| `@element-plus/icons-vue` | ArrowRight、ArrowRightBold（**必须显式 import**） |

配置项（参考 `src/config/index.js`）：

```js
config.busURL      // 业务网关，如 https://workup.oortcloudsmart.com:2443
config.frontURLStr // 控制台前端路由前缀，如 /bus/apaas-web（应用跳转地址拼接用）
config.ssoAppName / config.ssoAppId / config.ssoSecretKey // SSO 应用凭证（secretkey 属敏感信息，勿外传/勿提交公共仓库）
```

图标资产（从 `pc_ui/src/assets/img/svg` 拷入 `src/assets/navheader/`）：
`nine.svg`（九宫格）、`nine_more.svg`（菜单）、`fullscreen.svg`、`exit-fullscreen.svg`、`lang.svg`（语言）、`person.png`（默认头像）

## 三、文件清单

```
src/components/siteNew/NavHeader.vue                         # 导航头：按钮组 + SSO 登录检测 + 用户下拉 + 语言下拉
src/components/siteNew/components/commonRightPoPover.vue     # 九宫格弹层（动态应用列表）
src/components/siteNew/components/commonRightPopoverMore.vue # 菜单弹层（含当前场景入口）
src/components/siteNew/components/ChangeHYRight.vue           # 行业/场景/职能选择弹框
src/api/modules/industryScene.js                              # 场景列表、当前场景、保存接口
src/utils/industryScene.js                                    # 场景首页地址兼容与拼接
src/config/index.js                                          # busURL / frontURLStr / SSO 凭证
src/assets/navheader/*                                       # 图标
```

布局使用方（传入 `isLogin` prop）：`layouts/application.vue`、`layouts/siteNew.vue` 等传 true；`demo1.vue` 等传 false 隐藏登录区。

## 四、按钮组与间距

```html
<div class="flexRowAC nav_r">
  <!-- 全屏 -->
  <el-tooltip v-if="isLogin && !!userInfo" :content="isFullscreen ? '退出全屏' : '全屏'" placement="bottom">
    <img class="right_info_nine" :src="isFullscreen ? exitFullscreenIcon : fullscreenIcon" @click="toggle" />
  </el-tooltip>
  <!-- 语言切换（hover 下拉，见 §7） -->
  <el-dropdown v-if="isLogin && !!userInfo" class="lang_switch" trigger="hover" @command="handleLocaleCommand">...</el-dropdown>
  <!-- 未登录：登录/注册；登录后：欢迎您 + 用户下拉（见 §6） -->
  <NuxtLink v-if="!userInfo" :to="gotoLoginURL" target="_blank">...</NuxtLink>
  <template v-else>
    <span class="right_info_tips">欢迎您</span>
    <el-dropdown trigger="hover" @command="handleUserCommand">...</el-dropdown>
  </template>
  <!-- 九宫格、菜单（见 §8/§9） -->
  <el-popover v-if="isLogin && !!userInfo" placement="bottom" trigger="click" popper-class="popover_panel">...</el-popover>
  <el-popover v-if="isLogin && !!userInfo" v-model:visible="moreVisible" trigger="click" :width="260" popper-class="popover_panel_more">...</el-popover>
</div>
```

```scss
.nav_r {
  justify-content: end;
  width: auto;
  gap: 12px;          // 间距唯一来源，子元素 margin 全部归 0（避免双边 margin 叠加导致间隔不一致）
}
.right_info_nine {     // 图标按钮统一样式，hover 圆形高亮
  width: 30px; height: 30px; margin: 0; cursor: pointer;
  border-radius: 4px; transition: border-radius .5s ease;
  &:hover { border-radius: 50%; background-color: #00000015; }
}
```

要点：
- 所有弹框显示条件统一 `v-if="isLogin && !!userInfo"`（真实登录态，勿漏，否则未登录也会渲染）
- 间隔不要用双边 margin，容器 `gap` 一个值控制

## 五、SSO 登录态检测（NavHeader.vue）

### 5.1 存储

```js
import { useSessionStorage, StorageSerializers } from '@vueuse/core'
const accessTokenStorage = useSessionStorage('accessToken', '')
const tenantIdStorage   = useSessionStorage('tenantId', '')
// ★ 关键：初始值 null 会被 vueuse 推断为 any 序列化器，存对象会变成 "[object Object]"
const userInfoStorage   = useSessionStorage('userInfo', null, { serializer: StorageSerializers.object })
```

### 5.2 请求头（所有 SSO/业务接口通用）

```js
const getAuthHeaders = () => ({
  'Content-Type': 'application/json',
  tenantId: tenantIdStorage.value || '',
  accesstoken: accessTokenStorage.value || '',
  appid: config.ssoAppId,
  secretkey: config.ssoSecretKey,
  requesttype: 'app'
})
```

### 5.3 登录链路

1. 登录入口：`getLoginURL(redirectUri)` 拼 SSO 登录页地址（带 appId/secretkey/redirect）
2. 登录回跳 URL 带 `?access_token=` → 存入 token
3. `verifyToken`：POST `/bus/apaas-sso/sso/v1/verifyToken`，成功分支必须取 `res.data.accessToken` 与 `res.data.tenantId` 回存
4. `fetchUserInfo`：POST `/bus/apaas-sso/sso/v1/getUserInfo`，**res.data 全量存 userInfoStorage**（后续实名状态、user_id 都从这里取）
5. `checkLoginStatus`：verifyToken 通过 → fetchUserInfo；用 `isCheckingLogin` ref 防并发重入

### 5.4 保活与多 tab 同步

```js
onMounted(() => {
  checkLoginStatus()
  window.addEventListener('focus', handleWindowFocus)          // 窗口聚焦即校验
  window.addEventListener('storage', handleStorageChange)      // 监听 oortAccessToken/oortTenantId/oortLogoutAt
  verifyTimer.value = window.setInterval(checkLoginStatus, 10 * 1000)  // 10s 轮询
})
```

- 跨 tab 共享：登录态写入 `localStorage.oortAccessToken / oortTenantId`（SYNC_KEY），登出写 `oortLogoutAt` 时间戳
- `resetLoginState(clearSharedStorage)`：清 sessionStorage，可选清 localStorage 共享键
- 退出登录 `logoutSSO(needRedirectLogin)`：调 `/bus/apaas-sso/sso/v1/logout` → reset → 「切换账号」传 true 跳登录页，「退出登录」传 false 留在当前页

## 六、用户 hover 下拉卡片（NavHeader.vue）

```html
<el-dropdown trigger="hover" @command="handleUserCommand">
  <div class="user_info"><img :src="userPhoto" /><span>{{ userName }}</span></div>
  <template #dropdown>
    <el-dropdown-menu class="user_card_menu">
      <div class="user_card_head">
        <img :src="userPhoto" />
        <div class="user_card_head_info">
          <div class="user_card_name">{{ userName }}</div>
          <div class="user_card_tag" :class="{ 'is_real': realNameStatus === 1 }">
            {{ realNameStatus === 1 ? '已实名' : '未实名' }}
          </div>
        </div>
      </div>
      <el-dropdown-item command="switchAccount" class="user_card_action">
        <span>切换账号</span><el-icon><ArrowRight /></el-icon>
      </el-dropdown-item>
      <el-dropdown-item command="logout" class="user_card_action">
        <span>退出登录</span><el-icon><ArrowRight /></el-icon>
      </el-dropdown-item>
    </el-dropdown-menu>
  </template>
</el-dropdown>
```

```js
// 实名状态：user_ident_info.user_ident 中 identity_type === 2 项的 status（1=已实名）
const realNameStatus = computed(() => {
  const arr = userInfo.value?.user_ident_info?.user_ident || []
  const item = arr.find(i => i.identity_type === 2)
  return item ? item.status : 0
})
```

样式（**必须放全局（非 scoped）style 块**，dropdown 内容 teleport 到 body；PX 固定防 vw 缩放）：

```scss
.user_card_menu { width: 300PX !important; padding: 0 !important; border-radius: 10PX !important; overflow: hidden; }
.user_card_head { display: flex; align-items: center; padding: 20PX 16PX;
  img { width: 48PX; height: 48PX; border-radius: 50%; background: #e1e1e1; margin-right: 12PX; object-fit: cover; } }
.user_card_name { font-size: 16PX; font-weight: bold; color: #222; word-break: break-all; }
.user_card_tag { margin-top: 4PX; padding: 2PX 4PX; width: fit-content; background: #C7CFE2; font-size: 12PX; color: #fff; }
.user_card_tag.is_real { background: #0dcb69; }
.user_card_action.el-dropdown-menu__item { justify-content: space-between; padding: 12PX 16PX; font-size: 14PX;
  color: #222; background: #EDF3F9; border-top: 1PX solid #e3e9f0; }
.user_card_action.el-dropdown-menu__item:not(.is-disabled):hover,
.user_card_action.el-dropdown-menu__item:not(.is-disabled):focus { background: #dde3ea; color: #222; }
```

## 七、语言切换下拉

```html
<el-dropdown class="lang_switch" trigger="hover" @command="handleLocaleCommand">
  <img class="right_info_nine" :src="langIcon" alt="语言" />
  <template #dropdown>
    <el-dropdown-menu class="lang_menu">
      <el-dropdown-item command="zh" :disabled="currentLocale === 'zh'"><span>简体中文</span><el-icon><ArrowRight /></el-icon></el-dropdown-item>
      <el-dropdown-item command="en" :disabled="currentLocale === 'en'"><span>English</span><el-icon><ArrowRight /></el-icon></el-dropdown-item>
    </el-dropdown-menu>
  </template>
</el-dropdown>
```

```js
const currentLocale = computed(() => route.path.startsWith('/zh') ? 'zh' : 'en')
const handleLocaleCommand = (v) => {
  if (v === 'zh') { router.push('/zh/siteNew/'); return }
  window.location.href = '/en/'   // 官网既有约定（对齐 Bottom.vue）
}
```

样式同用户卡片行式（`width: 180PX`、`#EDF3F9` 底、分隔线、hover `#dde3ea`、当前语言 `is-disabled` 灰字 `#999`）。迁移时按目标站语言路由改两个跳转地址。

## 八、九宫格弹层（commonRightPoPover.vue）

### 8.1 壳（popper-class，全局样式）

```scss
.popover_panel {
  width: 400px !important;
  background-color: #EDF3F9 !important;
  border-radius: 10px !important;
  padding: 0 !important;               // ★ 必须去默认 padding，否则弹层内容外有一圈白边
}
.popover_panel .el-popper__arrow:before { background-color: #EDF3F9 !important; }
```

### 8.2 动态应用列表

```js
const getMyPCAppList = async() => {
  const res = await ofetch(config.busURL + '/bus/apaas-admin-platform/client/module/mypclist', {
    method: 'POST',
    body: {
      accessToken: accessTokenStorage.value,
      uuid: userInfoStorage.value?.user_id || userInfoStorage.value?.userId || '', // ★ 后端 required，必须真实 user_id
      pageNum: 1,
      pageSize: 999
    },
    headers: getAuthHeaders()
  })
  if (res.code === 200) {
    res.data.list.forEach(g => g.app_list && g.app_list.forEach(itd => appList.value.push(itd)))
  }
}
onMounted(() => { getMyPCAppList() })
```

注意：组件由 popper 内容 `v-if` 控制挂载时机（登录态就绪后才挂载，sessionStorage 必有值）。

### 8.3 应用跳转与更多应用

```js
const gotoApp = async(item) => {
  if (!(await verifyLogin())) { ElMessage.warning('用户信息失效， 请重新登录'); return }
  // open_mod: 1=_blank 2=_self 3=applabel；apk_url 带 http 直接开，否则拼 busURL + frontURLStr + apk_url
  // 追加参数：?accessToken=xxx&fromWhere=console_manage
}
const moreApps = async() => {
  // 跳应用市场：busURL + frontURLStr + '/app_market/index.html#/moreApp?accessToken=xxx&fromWhere=console_manage'
  // 同时写 sessionStorage.tempObj = { accessToken, fromWhere: 'console_manage' }
}
```

### 8.4 内容样式

```scss
.pover_right { border-radius: 4px; background-color: #EDF3F9; width: 100%; }
.pover_apps { display: flex; flex-wrap: wrap; width: 98%; margin: 0 auto; background-color: transparent; /* ★ 不要白底 */
  max-height: 50vh; overflow: auto; }
.pover_apps_item { width: 100px; height: 100px; display: flex; flex-direction: column; align-items: center;
  justify-content: center; margin: 8px 6px; cursor: pointer;
  img { height: 48px; width: auto; }
  div { margin-top: 10px; font-size: 14PX; /* ★ 大写 PX：不随视口 vw 放大 */ color: #3D3D3D; } }
.pover_apps_item:hover { background-color: #dde3ea; border-radius: 4px; }
.more_apps_tips { border: 1px solid #2278FF; border-radius: 136px; padding: 2px 12px; cursor: pointer;
  span { color: #2278FF; font-size: 14PX; } }
```

## 九、菜单弹层（commonRightPopoverMore.vue）

壳 `popper-class="popover_panel_more"`（`#EDF3F9` / `padding: 0` / 圆角 10px / 箭头同色，同 §8.1）。

菜单包含场景选择、隐私政策、切换账号、退出登录。场景入口会先请求用户配置，仅当行业、场景、职能任一列表有数据时显示；
入口文案显示当前生效配置的类型与名称，例如 `场景（智慧街道）`，名称为空时显示 `场景选择`。

场景入口（emit `moreOpr`，父组件 `handleMoreOpr` 分发）：

```html
<div v-if="hasIndustryConfig" class="pover_more_item" @click="emits('moreOpr', 'changeHY')">
  <div class="pover_more_item_title">
    <div>
      <img src="@/assets/navheader/popover_hy.png" />
      <span>场景（{{ selectedIndustryName }}）</span>
    </div>
    <el-icon><ArrowRightBold /></el-icon>
  </div>
</div>
```

其余菜单项：

```html
<div class="pover_more">
  <div class="pover_more_item" @click="emits('moreOpr', 'privacy')">
    <div class="pover_more_item_title">
      <div><img :src="privacyIcon" /><span>隐私政策</span></div>
      <el-icon><ArrowRightBold /></el-icon>
    </div>
  </div>
  <div class="pover_more_item" @click="emits('moreOpr', 'switchAccount')"> ...切换账号... </div>
  <div class="pover_more_item" @click="emits('moreOpr', 'logout')">
    <div class="pover_more_item_title">
      <div><img :src="logoutIcon" /><span style="color: #FF3C3C">退出登录</span></div>
      <el-icon><ArrowRightBold /></el-icon>
    </div>
  </div>
</div>
```

```js
// 父组件分发
const handleMoreOpr = (action) => {
  if (action === 'changeHY') {
    moreVisible.value = false
    industryVisible.value = true
    return
  }
  if (action === 'switchAccount') { logoutSSO(true); return }
  if (action === 'logout') { logoutSSO(false); return }
  if (action === 'privacy') { window.open('/zh/siteNew/privacy', '_blank') }
}
```

### 9.1 场景接口与状态

| 接口 | 入参 | 用途 |
| --- | --- | --- |
| `/bus/apaas-sso/userSceneManage/v1/getMyIndustryList` | `accessToken` | 获取 `hyConfig`、`cjConfig`、`znConfig` |
| `/bus/apaas-sso/userSceneManage/v1/getLastIndustry` | `accessToken`、`tenant_id`、`entity_id` | 获取当前生效配置 |
| `/bus/apaas-sso/userSceneManage/v1/saveLastIndustry` | `accessToken`、`tenant_id`、`industry_id`、`set_type` | 保存当前配置 |

三个接口沿用顶部登录链路的请求头：`tenantId`、`accesstoken`、`appid`、`secretkey`、`requesttype: app`。
`set_type` 取值：`1=行业`、`2=场景`、`3=职能`。

### 9.2 场景选择弹框

`NavHeader.vue` 收到 `changeHY` 后关闭菜单浮层并打开 `ChangeHYRight.vue`：

```html
<el-dialog v-model="industryVisible" title="场景选择" width="52%" top="5vh" destroy-on-close>
  <ChangeHYRight
    :access-token="accessToken"
    :tenant-id="tenantId"
    :user-info="userInfo"
    @close-dialog="industryVisible = false"
  />
</el-dialog>
```

弹框按“行业 / 场景 / 职能”分组展示卡片，包含名称、首页地址、说明、创建人、创建时间与生效按钮。当前项按钮显示
`当前生效` 并禁用；其他项点击后先调用 `saveLastIndustry`，成功后跳转配置首页。

首页字段兼容 `homepageUrl`、`homepage_url`、`homePageUrl`、`home_page_url`。绝对地址直接跳转；相对地址按
`config.busURL + config.frontURLStr + homepageUrl` 拼为控制台地址。若保存成功但未配置首页，则提示成功并关闭弹框。

接口失败时保留当前页面并显示中文错误提示；列表为空时显示“当前租户未配置相关配置”。场景图标来自
`src/assets/navheader/popover_hy.png`。

### 9.3 本次验证结果

- 已对本次涉及的 Vue/JS 文件执行定向 ESLint：`0 errors`。
- `NavHeader.vue` 保留 3 条迁移前已有的 `no-console` warning，本次新增文件无 lint 告警。
- 已执行 `git diff --check`，未发现空白符错误；场景图标与 pc_ui 源文件 SHA-256 一致。
- 未执行全量编译；场景接口及保存后真实跳转需使用有效登录账号联调验收。

行式项样式（scoped + 大写 PX）：

```scss
.pover_more_item { display: flex; flex-direction: column; box-sizing: border-box; /* ★ min-height 含 padding */
  min-height: 48PX; background-color: #EDF3F9; padding: 8PX 12PX;
  & + & { border-top: 1PX solid #e3e9f0; }          // 项间分隔线
  &:first-child { border-radius: 4PX 4PX 0 0; }
  &:last-child  { border-radius: 0 0 4PX 4PX; }
  &:hover { background-color: #dde3ea; }
  &_title { cursor: pointer; display: flex; align-items: center; justify-content: space-between; height: 32PX;
    div { display: flex; align-items: center;
      img { width: 24PX; height: 24PX; margin-right: 8PX; }
      span { font-size: 16PX; color: #575656; } } }
}
```

pc_ui 完整版菜单另有切换租户（运营管理平台）、反馈、主题色面板，仍依赖控制台专属 Store/配置，官网轻量版未纳入。
场景选择已完成官网适配：控制台 Store 改为由 `NavHeader` 显式传入 token、租户和用户信息，业务协议与交互保持一致。

## 十、迁移必踩的坑（重要）

1. **postcss-px-to-viewport 全局 vw 转换**：本项目 nuxt.config.ts 将所有小写 `px` 按 1920 基准转 vw（`selectorBlaskList` 拼写错误导致黑名单失效；1.1.1 版不支持 ignore 注释）。弹层是固定尺寸浮层，**一律用大写 `PX` 单位**（合法且等价 px，转换正则大小写敏感不会匹配）。页面版式布局仍用小写 px 随屏缩放。
2. **min-height 是 content-box**：不含 padding，需显式 `box-sizing: border-box`，否则 48PX 实际渲染 64PX。
3. **vueuse useSessionStorage 存对象**：初始值传 `null` 会被推断为 any 序列化器，写对象变成字符串 `"[object Object]"`，下游取 `user_id` 为空 → mypclist 报 `Uuid required`。读写两端都要 `{ serializer: StorageSerializers.object }`。
4. **teleport 弹层样式**：el-dropdown/el-popover 内容挂 body 下；壳定制类（popover_panel* / user_card_menu / lang_menu）必须放**全局（非 scoped）style**；弹层内容组件整体样式也建议全局块，避免困惑。
5. **element-plus 图标**：必须显式 `import { ArrowRight, ArrowRightBold } from '@element-plus/icons-vue'`；ESLint import 顺序要求第三方包在相对路径之前。
6. **el-popover 显示条件**：登录相关的 popover 必须加 `v-if="isLogin && !!userInfo"`，否则未登录也渲染。
7. **验证弹框必须真实交互**：el-dropdown/el-popover 对 dispatchEvent 模拟的 hover/click 无响应，自动化测试需用真实鼠标事件（或快照 uid + hover/click 工具）。
8. **语言跳转约定**：中文 `router.push('/zh/siteNew/')`，英文 `window.location.href = '/en/'`（与官网 Bottom 保持一致）；目标站按自身路由调整。
9. **测试登录态**：本地验证可劫持 `window.fetch` 伪造 verifyToken（返回 `data.accessToken/tenantId`）、getUserInfo（`data.user_name/photo/user_id/...`）、mypclist（`data.list`），再设置 `accessTokenStorage` 并调用 `checkLoginStatus()`；直接注入 userInfo 会被 10s 轮询清掉。

## 十一、验收清单

- [ ] 未登录：仅「登录/注册」按钮；登录回跳带 access_token 后正常进入登录态
- [ ] 登录后按钮顺序：全屏 → 语言 → 欢迎您 → 头像+用户名 → 九宫格 → 菜单；相邻间隔一致（gap 12px）
- [ ] 四个弹框风格统一：浅蓝底、壳无白边（padding 0）、圆角、右箭头、项 hover #dde3ea、分隔线
- [ ] 九宫格：Network 中 mypclist 请求体 uuid 有值（真实 user_id），应用列表正常渲染；点击应用先校验 token 再跳转；「更多应用」跳应用市场
- [ ] 菜单：有场景配置时显示当前配置，无配置时隐藏场景入口
- [ ] 场景选择：行业/场景/职能正确分组，当前项禁用，保存成功后跳转对应首页
- [ ] 菜单：隐私政策新开标签；切换账号登出并跳登录页；退出登录留当前页并清态
- [ ] 用户下拉：头像/名字/实名标签正确（实名绿 #0dcb69，未实名灰蓝 #C7CFE2）
- [ ] 语言切换：中文/英文互切正常，当前语言置灰
- [ ] 全屏/退出全屏图标与行为正确
- [ ] 宽屏（>1920）下弹层尺寸、字体不放大（PX 单位生效）
- [ ] 10s 轮询/token 失效时自动回到未登录态，多 tab 登出同步
